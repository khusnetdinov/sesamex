defmodule Mix.Tasks.Sesamex.Gen.ModelTest do
  use ExUnit.Case, async: true

  import Mix.Tasks.Sesamex.Gen.Model

  describe "Mix.Tasks.Sesamex.Gen.Model" do
    @tag :skip
    test "should create model and migration" do
    end
  end
end
